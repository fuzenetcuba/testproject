{% extends 'BackendBundle::layout.html.twig' %}

{% block custom_css %}
    {{ parent() }}


    <!-- iCheck Styles -->
    <link rel="stylesheet" href="{{ asset('bundles/backend/css/plugins/iCheck/custom.css') }}"
          type="text/css"/>

    <!-- select2 Styles -->
    <link rel="stylesheet" href="{{ asset('bundles/backend/css/plugins/select2/select2.min.css') }}"
          type="text/css"/>

{% endblock %}

        {% block active_feedback %}active{% endblock %}

        {% block page_heading_title %}
            <a class="btn btn-default navbar-btn" href="{{ path('feedback') }}"
                >
                <span class="glyphicon glyphicon-chevron-up"></span>
            </a>
            {{ 'Feedback edition' | trans({}, 'feedbackbackend') }}
        {% endblock %}

        {% block page_content %}

            {{ form_start(edit_form, {'attr': {'class':'form-horizontal'}}) }}



            <div class="form-group{% if(edit_form.satisfied.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.satisfied) }}</label>

                <div class="col-md-1">
                    <div class="checkbox-inline">
                        {{ form_widget(edit_form.satisfied, { 'attr': {'class' : 'form-control i-checks'}}) }}
                        {% if edit_form.satisfied.vars['errors']|length > 0 %}
                            <ul class="message">
                                {% for error in edit_form.satisfied.vars['errors'] %}
                                    <li>{{ error.message }}</li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.recommended.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.recommended) }}</label>

                <div class="col-md-1">
                    <div class="checkbox-inline">
                        {{ form_widget(edit_form.recommended, { 'attr': {'class' : 'form-control i-checks'}}) }}
                        {% if edit_form.recommended.vars['errors']|length > 0 %}
                            <ul class="message">
                                {% for error in edit_form.recommended.vars['errors'] %}
                                    <li>{{ error.message }}</li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.whyNotRecommend.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.whyNotRecommend) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.whyNotRecommend, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.whyNotRecommend.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.whyNotRecommend.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.storesSatisfied.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.storesSatisfied) }}</label>

                <div class="col-md-1">
                    <div class="checkbox-inline">
                        {{ form_widget(edit_form.storesSatisfied, { 'attr': {'class' : 'form-control i-checks'}}) }}
                        {% if edit_form.storesSatisfied.vars['errors']|length > 0 %}
                            <ul class="message">
                                {% for error in edit_form.storesSatisfied.vars['errors'] %}
                                    <li>{{ error.message }}</li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.additionalStores.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.additionalStores) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.additionalStores, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.additionalStores.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.additionalStores.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.foodSatisfied.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.foodSatisfied) }}</label>

                <div class="col-md-1">
                    <div class="checkbox-inline">
                        {{ form_widget(edit_form.foodSatisfied, { 'attr': {'class' : 'form-control i-checks'}}) }}
                        {% if edit_form.foodSatisfied.vars['errors']|length > 0 %}
                            <ul class="message">
                                {% for error in edit_form.foodSatisfied.vars['errors'] %}
                                    <li>{{ error.message }}</li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.additionalFood.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.additionalFood) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.additionalFood, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.additionalFood.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.additionalFood.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.rate.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.rate) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.rate, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.rate.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.rate.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.name.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.name) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.name, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.name.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.name.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.email.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.email) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.email, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.email.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.email.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>



            <div class="form-group{% if(edit_form.phone.vars['errors']|length > 0) %} {{ ' has-error' }} {% endif %}">
                <label for="" class="col-md-2 control-label">{{ form_label(edit_form.phone) }}</label>

                <div class="col-md-6">
                    {{ form_widget(edit_form.phone, { 'attr': {'class' : 'form-control'}}) }}
                    {% if edit_form.phone.vars['errors']|length > 0 %}
                        <ul class="message">
                            {% for error in edit_form.phone.vars['errors'] %}
                                <li>{{ error.message }}</li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                </div>
            </div>
            <div class="hr-line-dashed"></div>




            <div class="form-group">
                <div class="col-md-2 text-right">
                    <a class="btn btn-success" href="{{ path('feedback') }}">
                        <span class="glyphicon glyphicon-chevron-left"></span>
                        {{ 'Back to the list' | trans({}, 'generalbackend') }}
                    </a>
                </div>
                <div class="col-md-10">


                    {{ form_widget(edit_form.submit, { 'attr': { 'class' : 'btn btn-primary btn-small', }}) }}


                    <a class="btn btn-danger" data-toggle="modal" data-target="#modalDelete">
                        <span class="fa fa-remove"></span>
                        {{ 'Delete' | trans({}, 'generalbackend') }}
                    </a>


                    {{ form(edit_form) }}
                </div>
            </div>


            <div class="modal inmodal" id="modalDelete" tabindex="-1" role="dialog" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content animated bounceInRight">
                        <div id="modal-title" class="modal-header red-bg">
                            <button type="button" class="close" data-dismiss="modal"><span
                                        aria-hidden="true">&times;</span><span class="sr-only">{{ 'Close' | trans({}, 'generalbackend') }}</span>
                            </button>
                            <i class="fa fa-exclamation-triangle modal-icon text-white"></i>
                            <h4 class="modal-title">{{ 'Attention' | trans({}, 'generalbackend') }}!!!</h4>
                        </div>
                        <div id="modal-content" class="modal-body">
                            <strong>{{ 'Are you sure you want to delete this data?' | trans({}, 'generalbackend') }}</strong>
                        </div>
                        <div class="modal-footer">
                            <form action="{{ path('feedback_delete', { 'id': entity.id }) }}"
                                  name="form-delete" method="post">
                                <button type="button" class="btn btn-white" data-dismiss="modal">{{ 'Close' | trans({}, 'generalbackend') }}</button>
                                <button id="modal-btn" type="submit" class="btn btn-danger" name="feedback-id"
                                        value="{{ entity.id }}">
                                    <span class="glyphicon glyphicon-ok"></span> {{ 'Save changes' | trans({}, 'generalbackend') }}
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>


            {{ form_end(edit_form) }}

        {% endblock %}

    {% block custom_js %}
        {{ parent() }}

        <!-- iCheck Scripts -->
        <script src="{{ asset('bundles/backend/js/plugins/iCheck/icheck.min.js') }}"
                type="text/javascript"></script>

        <!-- select2 Scripts -->
        <script src="{{ asset('bundles/backend/js/plugins/select2/select2.full.min.js') }}"
                type="text/javascript"></script>
    {% endblock %}

    {% block inline_js %}
        <script type="text/javascript">

            $(document).ready(function () {


                //  iCheck scripts for checkboxes and radios
                $(".i-checks").iCheck({
                    checkboxClass: "icheckbox_square-green",
                    radioClass: "iradio_square-green",
                });

                //  select2 scripts for batch actions combobox
                $(".select2-field").select2({
                    placeholder: "--- {{ 'Select a value' | trans({}, 'generalbackend') }} ---"
                });


            });

        </script>
    {% endblock %}
